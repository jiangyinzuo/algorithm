#include <cstdio>
#include <cstdint>

static int r[100002];
static int64_t d[100002];

int main() {
  int T;
  scanf("%d", &T);
  while (T--) {
	int n, q;
	scanf("%d %d", &n, &q);
	for (int i = 1; i <= n; ++i) {
	  scanf("%d", r + i);
	}
	for (int i = 1; i <= n; ++i) {
	  for (int j = 1; j <= n; ++j) {

	  }
	}
  }
  return 0;
}
